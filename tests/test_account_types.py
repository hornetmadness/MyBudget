import pytest
from fastapi.testclient import TestClient


# Account types tests removed - AccountType table has been removed
# Account types are now stored as strings in the bank_account.account_type field

